# SnapScan PDF (Scan-Pdf-Summary) 요구사항명세서

## 보류 상태

**이 프로젝트는 2026년 12월까지 작업을 보류(hold)합니다.**

- 보류 시작: 2026-09-21
- 재개 예정: 2026-12 이후
- 이 문서와 `CLAUDE.md`에 보류 상태를 남겨서, 이 시점 이후 이 프로젝트를 다시 열어
  작업을 요청할 경우 그 사실을 먼저 상기시키도록 해둠.

## 프로젝트 개요

사진을 찍으면 문서 경계 자동 인식 → 원근 보정 → OCR 텍스트 인식을 거쳐 검색 가능한
PDF로 만들어주는 React Native(Expo) 모바일 앱. 여기에 더해 Claude API로 문서 내용을
요약한 별도의 AI 요약 PDF도 함께 생성한다. iOS/Android 공용 코드베이스(Android 중심으로
테스트됨).

## 주요 기능

1. **촬영 → 검색 가능한 PDF 생성**
   - 네이티브 문서 스캐너(`react-native-document-scanner-plugin`)로 촬영, 경계 인식/원근
     보정
   - 여러 페이지 연속 촬영 → 순서 변경/삭제 후 한 PDF로 합치기
   - `@shopify/react-native-skia` 기반 적응형 이진화로 그림자/구겨짐 보정
   - 온디바이스 OCR(ML Kit, 한국어) → 이미지 위에 투명 텍스트 레이어를 겹쳐 PDF 안에서
     텍스트 선택/검색 가능
   - OCR 텍스트 첫 줄로 문서 제목 자동 생성

2. **AI 요약 PDF (Claude API)**
   - 촬영한 문서의 OCR 텍스트, 또는 기존 PDF 파일 자체(Claude가 PDF를 직접 읽음)를
     Claude API로 보내 번호 매긴 요약을 생성
   - 원본 스캔 PDF와는 독립된 별도 PDF 파일로 저장
   - Anthropic API 키는 설정 화면에서 등록, 기기에 `expo-secure-store`로 저장(앱 삭제 시
     함께 삭제됨)

3. **기존 PDF 요약(가져오기)**
   - 로컬 폴더에서 선택: 폴더를 고르면 그 안 PDF 목록을 체크박스로 다중 선택해 한 번에
     요약, 같은 폴더에 자동 저장 시도
   - 클라우드 드라이브(Google Drive 등)에서 선택: 시스템 파일 선택창으로 다중 선택 가능,
     클라우드 폴더는 SAF 쓰기 제한으로 자동 저장이 안 되므로 요약 후 공유/폴더 선택 저장을
     사용자가 직접 진행

4. **저장/공유**
   - 공유 시트(다른 앱으로 공유, "파일에 저장")
   - Android: 폴더 선택 다이얼로그(SAF)로 직접 저장 — 단, 구글 드라이브처럼 SAF 쓰기를
     지원하지 않는 폴더는 실패 시 안내 메시지로 대체 경로(공유) 유도

5. **네비게이션 구조**
   - 모든 화면 공통 헤더: 앱 이름 "Scan-Pdf-Summary" + 3개 작업 카테고리(① 기존 PDF 요약
     ② 촬영해서 요약 ③ 기존 작업내용) 상시 노출, 탭처럼 전환
   - 폰 하드웨어/제스처 뒤로가기 → 항상 홈으로 즉시 이동

## 화면 구성 (`src/screens/`)

- `HomeScreen.tsx` — 3개 메뉴 진입점
- `ScanScreen.tsx` — 네이티브 스캐너 실행
- `PageEditorScreen.tsx` — 페이지 편집, PDF/AI 요약 PDF 생성
- `PdfPreviewScreen.tsx` — 문서 상세, 원본/요약 PDF 각각 공유·저장, 삭제
- `MyDocumentsScreen.tsx` — 저장된 문서 목록("기존 작업내용")
- `ImportSummaryScreen.tsx` — 기존 PDF 요약(로컬/클라우드)
- `SettingsScreen.tsx` — Anthropic API 키 등록/삭제

## 기술 스택 요약

- Expo(React Native, TypeScript), EAS Build(Android `preview` 프로필 = APK)
- `react-native-document-scanner-plugin`, `@react-native-ml-kit/text-recognition`,
  `@shopify/react-native-skia`, `expo-print`, `expo-file-system`(SAF 포함), `expo-sharing`,
  `expo-document-picker`, `expo-secure-store`, `@react-navigation`,
  `@react-native-async-storage/async-storage`
- Anthropic Messages API(`fetch` 직접 호출, 모델 `claude-opus-5`) — 클라이언트에서 직접
  호출, 별도 백엔드 프록시 없음(개인 사용 목적으로는 무방하나, 배포 확대 시 프록시 구조
  전환 권장)

## 알려진 제한 사항 / 재개 시 확인할 것

- Google Drive 등 클라우드 폴더는 SAF로 새 파일을 직접 쓸 수 없음(OS 제약) — 공유 경로로
  우회 중
- Android 시스템 파일 선택창은 앱이 "로컬만/클라우드만" 보이게 강제할 수 없음(OS가 관리)
  — 로컬 폴더 선택은 다운로드 폴더를 기본 시작 위치로만 유도
- EAS 무료 플랜 월간 Android 빌드 횟수 제한 있음(매월 초기화)
- API 키가 클라이언트에 저장되는 구조라 개인 사용 전용, 배포 확대 시 백엔드 프록시 필요
- `README.md`에 기술 스택/화면 구성/설치 방법 상세 기재됨 — 재개 시 README부터 확인
