# 요구사항 명세서

## 1. 커뮤니케이션 규칙

| 항목 | 내용 |
| --- | --- |
| AI 작업 설명 언어 | AI(Claude Code 등)가 이 저장소에서 작업을 수행한 뒤, 그 실행 내용에 대한 설명(사용자에게 보고하는 답변, 진행 상황 안내 등)은 **한글로 작성**한다. |
| 영어 약자/합성어 풀이 | 사용자 질문에 AI가 답변할 때, 답변에 포함된 **영어 약자(예: ESLint, PDF, OCR)나 합성어**는 그냥 쓰지 않고 무엇의 줄임말인지/무슨 뜻인지 함께 풀어서 설명한다. |

## 2. 버전 관리 (롤백 지점)

앱인토스(AppinToss) 미니앱 전환 작업을 시작하기 전, 되돌아갈 수 있는 안정 버전을 아래에
기록한다. 전환 작업 중 문제가 생기면 이 커밋으로 롤백(원래 상태로 되돌리기)한다.

| 항목 | 내용 |
| --- | --- |
| 기록 일시 | 2026-09-20 |
| 브랜치 | `claude/scan-document-summary-questions-wr39l5` |
| 커밋 해시 | `0f80b4364aab77d70f2c8e9b9420e7ca0f538e2b` |
| 커밋 메시지 | Require spelling out English abbreviations/compound words in AI replies |
| 상태 요약 | 일반 Expo(React Native) 프로젝트. 카메라 스캔 → OCR → 검색 가능한 PDF 생성, Claude API 기반 AI 요약(교과서 판단 시 예상문제 포함) 기능까지 구현 완료. 앱인토스 관련 코드는 아직 없음(순수 Expo 상태). |
| 롤백 방법 | `git checkout 0f80b4364aab77d70f2c8e9b9420e7ca0f538e2b` 또는 해당 커밋을 가리키는 태그/브랜치를 만들어 되돌아간다. |

## 3. 개발 지침 (레퍼런스)

| 항목 | 내용 |
| --- | --- |
| 앱인토스 연동 개발 지침 | 이 프로젝트를 앱인토스(AppinToss, 토스 앱 안에서 실행되는 미니앱을 등록·배포하는 플랫폼) 미니앱으로 전환/등록하는 모든 작업은 **앱인토스 개발자센터** 공식 문서(https://developers-apps-in-toss.toss.im)를 개발 지침으로 삼아 진행한다. 문서와 실제 구현이 다르면, 실제로 npm에 배포된 패키지의 타입 정의(설치해서 `tsc`로 확인한 것)를 우선한다 — 문서가 아직 배포 전인 최신 기능을 앞서 설명하는 경우가 있기 때문이다(예: `openPDFViewer`는 문서에는 있지만 2026-09-20 기준 배포된 `@apps-in-toss/framework`에는 없었음). |

## 4. 앱인토스 전환 현황 (2026-09-20)

Granite 프레임워크(`@granite-js/react-native` + `@apps-in-toss/framework`) 기반으로
전체 재개발을 완료했다. 기존 Expo 버전과 비교했을 때 아래 기능은 **의도적으로 축소**했다
(앱인토스 SDK에 대응 API가 없어서):

| 기존(Expo) 기능 | 앱인토스 버전 처리 |
| --- | --- |
| 문서 경계 자동 인식 + 원근 보정 (`react-native-document-scanner-plugin`) | 없음. `openCamera`로 찍은 사진을 그대로 사용한다. |
| 온디바이스 OCR (`@react-native-ml-kit/text-recognition`) | 없음. OCR 텍스트를 미리 추출하지 않고, 촬영한 사진들로 만든 PDF를 Claude에 통째로 보내(PDF를 네이티브로 읽을 수 있음) 요약/예상문제를 받는다. |
| 검색 가능한 원본 PDF(이미지 위에 투명 텍스트 레이어) | 없음. `pdf-lib`로 사진만 담은 PDF를 만든다 — 텍스트 선택/검색은 안 된다. |
| PDF를 폴더/AsyncStorage에 영구 저장 후 다시 불러보기 | 없음. 앱인토스에는 저장한 파일을 다시 읽어오는 API가 없다(`saveBase64Data`는 기기에 내려주기만 함). 대신 AI 요약/예상문제 **텍스트**만 `Storage`(로컬 저장소)에 남기고, 목록 화면에서 "다시 PDF로 저장"을 누르면 그 텍스트로 PDF를 즉석에서 다시 만든다. |
| 기존 PDF 가져와서 요약(Android 폴더 선택, iOS 파일 선택) | 없음. 앱인토스에 폴더/파일 선택 API가 확인되지 않아 이번 전환에서는 제외했다. |
| `expo-secure-store` 하드웨어 보안 저장 | 앱인토스 `Storage`(일반 로컬 저장소, 토스 앱 삭제 시 함께 삭제)로 대체 — expo-secure-store만큼 안전하지는 않다. |

**한글 PDF 텍스트 렌더링**: `pdf-lib`의 내장 표준 폰트는 한글 글리프가 없어서, 전체
한글 음절(가~힣)과 기본 라틴/문장부호만 남긴 서브셋 폰트를 `assets/fonts/NotoSansKR-Regular.ttf`
(약 2.7MB, Google Fonts의 Noto Sans KR을 `fonttools`로 서브셋)로 번들에 포함하고
`@pdf-lib/fontkit`으로 심었다.

**아직 실기기/실제 앱인토스 샌드박스에서 검증하지 못한 부분**: 이 저장소는 네트워크가
제한된 환경에서 작성되어 `granite dev`/`granite build`를 실제로 실행해보지 못했다.
`tsc --noEmit`과 `eslint`는 통과했지만, 폰트 자산을 `require` + `Image.resolveAssetSource`
+ `fetch(uri)`로 읽어오는 방식이 실제 Granite Metro 번들러에서도 그대로 동작하는지는
실기기/샌드박스 테스트가 필요하다.
